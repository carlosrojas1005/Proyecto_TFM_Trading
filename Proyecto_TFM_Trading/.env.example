# IBKR paper by default (NO LIVE)
IB_HOST=127.0.0.1
IB_PORT=7497          # paper TWS por defecto
IB_CLIENT_ID=1001
PAPER=true            # <- siempre en true en el MVP

# LLM (placeholder) - no se usa en stub
LLM_PROVIDER=local
LLM_API_KEY=changeme
